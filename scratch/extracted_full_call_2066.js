"                else if (action === 'agenda') { if (block.length) openAgendaDialog(block[0]); }\n                else if (action === 'alert') { if (block.length) openAlertDialog(block[0], function(res) { block[0].dataset.alertEnabled = res.alertEnabled ? 'true' : 'false'; block[0].dataset.alertValue = res.alertValue; block[0].dataset.alertUnit = res.alertUnit; block[0].dataset.alertFired = 'false'; paintCard(block[0]); persist(); }); }"
